function sort(array) {
    var x = array.length;
    for (var i = 0; i < x - 1; i++) {
        var y = x - 1;

        for (var j = 0; j < y - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                temp = arr[j];
                arr[j]
            }
        }
    }
}

sort[4,3,5,6];
